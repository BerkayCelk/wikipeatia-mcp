# Hermes Agent (https://github.com/NousResearch/hermes-agent) example snippet.
# Add under your config.yaml's `mcp_servers` section, then reload MCP.
mcp_servers:
  wikipeatia:
    command: uvx
    args:
      - --from
      - git+https://github.com/BerkayCelk/wikipeatia-mcp
      - wikipeatia-mcp
    timeout: 30
    enabled: true
